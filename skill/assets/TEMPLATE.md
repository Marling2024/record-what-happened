---
task_id: "TASK-0001"
session_id: ""
title: ""
date: "YYYY-MM-DD"
start_time: "HH:MM"
end_time: "HH:MM"
duration: ""
status: "completed"
tags: []
related_tasks: []
key_files: []
prev_task: ""
---

# 任务：{标题}

## 1. 任务上下文

### 1.1 原始指令
> {用户给出的任务原文，原样保留，不做润色}

### 1.2 任务拆解
| 序号 | 子任务           | 依赖  | 状态       |
|------|------------------|-------|------------|
| 1    | {子任务描述}     | -     | done       |
| 2    | {子任务描述}     | 1     | done       |
| 3    | {子任务描述}     | 1, 2  | skipped    |

### 1.3 完成标准
- [ ] {明确、可验证的完成条件 1}
- [ ] {明确、可验证的完成条件 2}

---

## 2. 执行轨迹

### 2.1 步骤概要（顺利步骤合并区）
- HH:MM — 读取了 `src/config.ts`，确认使用 X 框架
- HH:MM — 运行 `npm run build`，通过
- HH:MM — {更多顺利步骤…}

### 2.2 关键步骤 1 — {简短动作描述}
- **时间**：HH:MM — HH:MM
- **动作**：{调用了什么工具 / 读写了什么文件 / 跑了什么命令}
- **决策理由**：{为什么这么做，尤其是分叉路口的判断依据}
- **假设**：{基于什么假设行动的}
- **结果**：{成功 / 失败 / 部分成功 + 简述}
- **产出**：{产生了什么文件/变更，无则填 -}

### 2.3 关键步骤 2 — {简短动作描述}
- **时间**：HH:MM — HH:MM
- **动作**：
- **决策理由**：
- **假设**：
- **结果**：
- **产出**：

<!-- 关键步骤块按需复制。仅展开关键决策点和异常处，顺利步骤合并到 2.1 概要区。 -->

---

## 3. 遇到的问题与解决

### 问题 1 — {问题简述}
- **发现时间**：HH:MM
- **现象**：
  > {报错原文 / 异常行为 / 偏离预期的表现，原样保留；如含敏感信息已脱敏}

- **根因分析**：
  {区分表象和根因。如"报错是找不到模块，根因是 tsconfig paths 未配置"}

- **尝试过的方案**：
  | 序号 | 方案                | 结果         | 备注               |
  |------|---------------------|--------------|--------------------|
  | 1    | {尝试 A}            | 失败         | {为什么失败}       |
  | 2    | {尝试 B}            | 失败         | {为什么失败}       |
  | 3    | {最终方案}          | 成功         | {为什么有效}       |

- **最终解决方案**：
  {具体改了什么 / 做了什么，引用文件路径和行号}

- **遗留问题**：
  - [ ] {暂时绕过但未根治的问题，标记 TODO}

### 问题 2 — {问题简述}
<!-- 按问题块复制 -->

---

## 4. 信息发现

### 4.1 关键发现
- {执行中了解到的重要事实，如"项目实际使用 Vite 而非 Webpack"}
- {配置文件实际路径与文档不符：实际在 X，文档说在 Y}

### 4.2 与预期不符之处
- {文档说一套、代码做一套的地方}

### 4.3 反模式 / 陷阱（下次注意）
- {踩过的坑 + 避让方法，如"该项目的 lint 会拒绝 any 类型，不要用"}

<!-- 若本次任务确实无实质性发现，可在此标注「无」，不必强行编造。 -->

---

## 5. 验证与结果

### 5.1 验证方式
| 序号 | 验证手段                          | 结果   | 证据 / 日志摘要          |
|------|-----------------------------------|--------|--------------------------|
| 1    | {如：运行 `npm test`}            | pass   | {5 passed, 0 failed}    |
| 2    | {如：手动检查输出文件}            | pass   | {文件存在且格式正确}     |
| 3    | {如：`npm run lint`}             | pass   | {0 errors}              |

### 5.2 产出物清单
| 文件路径                          | 操作   | 作用                         |
|-----------------------------------|--------|------------------------------|
| `src/utils/parser.ts`            | 新增   | {一句话说明}                 |
| `src/config/index.ts`            | 修改   | {改了什么}                   |
| `tests/parser.test.ts`           | 新增   | {对应测试}                   |

### 5.3 完成标准核对
- [x] {条件 1}
- [x] {条件 2}
- [ ] {未完成条件 — 说明原因}

---

## 6. 回顾笔记（可选，任务结束后补充）

### 做得好的
- {值得保留的做法}

### 可改进的
- {下次可以做得更好的地方}

### 如果重来会怎么做
- {复盘后的最优路径}

---

## 附：命令速查（本任务用到的关键命令）
<!-- 在此记录本次任务中反复使用的关键命令，仅作备忘，不要执行。若无则删除本节。 -->
```bash
# {命令 1}
# {命令 2}
```
